db.students.update(
                { $gt : {age: 0}},    // updates all > 0
                { $inc : { age : 1} }
                { multi : true}      // needed for multiple entries
)
                
                
db.students.update(
                { $eq : {name: "Alice"}},    // updates all > 0
                { $set : { grade : [..., NEW]} }
);


db.***.find({}).pretty()